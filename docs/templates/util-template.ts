import { Locator, Page } from '@playwright/test';

/**
 * UtilsTemplate: brief description of the utility purpose.
 * Keep methods concise and well-documented.
 */
export class UtilsTemplate {
  /**
   * Example helper method.
   * @param page Playwright page instance.
   * @param target Locator or selector to interact with.
   */
  static async example(page: Page, target: Locator) {
    // implement helper logic
  }
}
